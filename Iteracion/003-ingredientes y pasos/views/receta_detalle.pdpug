a(href="index.php") ← Volver al listado

h2 #{receta.titulo}
p #{receta.descripcion}
small Slug: #{receta.slug}

h3 Ingredientes
@if ingredientes
  ul
    @foreach ingredientes as ing
      li #{ing.texto}
@else
  p (Sin ingredientes todavía)

h3 Pasos
@if pasos
  ol
    @foreach pasos as st
      li #{st.texto}
@else
  p (Sin pasos todavía)